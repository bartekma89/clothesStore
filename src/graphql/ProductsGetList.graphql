query ProductsGetList($skip: Int, $take: Int) {
	products(skip: $skip, first: $take) {
		id
		name
		description
		categories(first: 1) {
			id
			name
		}
		images(first: 1) {
			url
		}
		price
	}
}
